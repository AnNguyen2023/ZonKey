[package]
name = "zonkey-telex"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
zonkey-token = { path = "../zonkey-token" }
zonkey-types = { path = "../zonkey-types" }

[dev-dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[lints]
workspace = true
